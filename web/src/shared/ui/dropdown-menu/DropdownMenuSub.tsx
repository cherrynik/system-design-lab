import { Menu } from '@mantine/core';
import type { DropdownMenuSubProps } from './dropdown-menu.types';

export function DropdownMenuSub(props: DropdownMenuSubProps) {
  return <Menu.Sub {...props} />;
}
